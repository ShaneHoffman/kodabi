import { PlaceholderView } from "./PlaceholderView";

export function CaptureView() {
  return (
    <PlaceholderView
      title="Quick capture"
      caption="A global hotkey will pop a lightweight text box in front of the routing pipeline. Captured thoughts land in a project or the Inbox."
      detail="Arrives with #45"
    />
  );
}
