import { PlaceholderView } from "./PlaceholderView";

export function InboxView() {
  return (
    <PlaceholderView
      title="Inbox"
      caption="Notes the router couldn't place with confidence land here, waiting for a one-click re-route."
      detail="Arrives with #44"
    />
  );
}
